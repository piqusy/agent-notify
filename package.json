{
  "name": "agent-notify-workspace",
  "private": true,
  "workspaces": ["packages/core", "packages/opencode", "cli"],
  "scripts": {
    "build": "tsup",
    "test":  "vitest run",
    "dev":   "tsup --watch",
    "typecheck": "tsc --build --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsup":       "^8.0.0",
    "vitest":     "^2.0.0"
  }
}
