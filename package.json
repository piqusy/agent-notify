{
  "name": "agent-notify-workspace",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/opencode",
    "cli"
  ],
  "scripts": {
    "build:macos-helper": "node scripts/build-macos-helper.mjs",
    "build": "bun run build:macos-helper && bun run --filter @agent-notify/core build && bun run --filter opencode-agent-notify build && bun run --filter agent-notify-cli build",
    "build:binary": "bun run build && bun build cli/src/index.ts --compile --outfile dist/agent-notify --target bun",
    "build:binary:arm64": "bun run build && bun build cli/src/index.ts --compile --outfile dist/agent-notify-darwin-arm64 --target bun --target-arch arm64",
    "build:binary:x64": "bun run build && bun build cli/src/index.ts --compile --outfile dist/agent-notify-darwin-x64 --target bun --target-arch x64",
    "check:release": "node scripts/check-release.mjs",
    "test": "vitest run",
    "typecheck": "bun run --filter @agent-notify/core build && bun run --workspaces typecheck"
  },
  "devDependencies": {
    "@types/node": "25.5.2",
    "postcss": "8.5.10",
    "tsup": "8.5.1",
    "typescript": "5.9.3",
    "vitest": "4.1.5"
  }
}
